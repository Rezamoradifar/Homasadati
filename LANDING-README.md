# همای سعادت — راه‌اندازی لندینگ کامل

پروژه شامل لندینگ واکنش‌گرا، زبان‌های فارسی/عربی/انگلیسی، فوتر قابل استفادهٔ مجدد، سرور Next.js، پایگاه دادهٔ SQLite و پنل مدیریت است.

## اجرای محلی

Node.js 22 LTS و npm لازم است. در پوشهٔ پروژه:

```bash
npm ci
npm run setup
npm run dev
```

سایت: http://localhost:3000 — مدیریت: http://localhost:3000/admin

دستور setup یک رمز تصادفی در `.env.local` می‌سازد. مقدار `ADMIN_PASSWORD` را از همان فایل برای ورود مدیر بردارید؛ آن را منتشر نکنید. اگر فایل از قبل وجود داشته باشد، setup آن را تغییر نمی‌دهد؛ متغیرها را طبق `.env.example` تکمیل کنید.

## بخش‌های فعال

- منو، کارت‌های خدمات، داستان برند، گالری با کلیدهای چپ/راست، پنجره‌های جزئیات و تمام لینک‌های فوتر.
- ثبت واقعی درخواست خدمات و درخواست عضویت باشگاه در SQLite؛ اعتبارسنجی سرور، رضایت برای ذخیرهٔ اطلاعات، جلوگیری از ثبت تکراری هنگام تلاش مجدد.
- رسید قابل دانلود و کد خصوصی پیگیری؛ مشاهدهٔ وضعیت بدون نمایش اطلاعات شخصی.
- ثبت درخواست خبرنامه و پیوند خصوصی لغو. ایمیل‌ها تأییدشده نیستند و ارسال خودکار ایمیل انجام نمی‌شود.
- پنل رمزدار: فهرست و صفحه‌بندی درخواست‌ها، تغییر وضعیت، فهرست خبرنامه و خروجی CSV صفحهٔ جاری.
- انتخاب زبان و ارز در مرورگر حفظ می‌شود؛ ارز انتخابی همراه درخواست ثبت می‌شود. هیچ نرخ تبدیل یا قیمت خریدی نمایش داده نمی‌شود.
- فوتر RTL/LTR، آکاردئون موبایل، ناوبری صفحه‌کلید، focus ring، حرکت ملایم و پشتیبانی از کاهش حرکت.

## اجرای production

```bash
npm run build
npm start
```

روی سرور Node دارای دیسک پایدار اجرا کنید. `DATABASE_PATH` را روی مسیر خصوصی و قابل نوشتن قرار دهید، `APP_ORIGIN` را دقیقاً برابر مبدأ HTTPS سایت و `ADMIN_PASSWORD` را رمزی طولانی و تصادفی تنظیم کنید. کوکی مدیریت در production فقط روی HTTPS ارسال می‌شود.

این نسخه مخصوص یک سرویس Node با SQLite روی دیسک محلی است؛ روی هاست صرفاً استاتیک، Cloudflare Workers یا فایل‌سیستم موقت serverless قابل استفاده نیست. چند replica با SQLite مستقل، داده‌های مشترک ندارند. برای توسعهٔ چندسروری باید پایگاه دادهٔ اشتراکی جایگزین شود.

از فایل SQLite با روش backup خود SQLite نسخهٔ پشتیبان بگیرید؛ هنگام فعال بودن WAL صرفاً کپی کردن یک فایل DB کافی نیست. رمز مدیریت را تغییر دهید و نشست‌های قدیمی را هنگام چرخش رمز حذف کنید. نشست‌ها هشت ساعت اعتبار دارند. تاریخ انقضای خودکار داده‌های درخواست تعریف نشده است.

پیش‌فرض rate limit بین همهٔ بازدیدکنندگان مشترک است. فقط پشت reverse proxy مورد اعتماد که هدر X-Forwarded-For را بازنویسی می‌کند `TRUST_PROXY=1` بگذارید. برای سایت عمومی، کنترل ترافیک و ضدربات در لایهٔ proxy را تنظیم کنید.

## اتصال اطلاعات کسب‌وکار

رزرو قطعی، پرداخت و ارسال ایمیل به سرویس‌های واقعی کسب‌وکار نیاز دارند. این نسخه درخواست را ذخیره می‌کند؛ خرید، پرداخت یا ایمیل موفق را شبیه‌سازی نمی‌کند. لینک‌های رسمی شبکه‌های اجتماعی، روش‌های پرداخت و گواهی‌های معتبر را با props فوتر اضافه کنید؛ ردیف‌ها وقتی دادهٔ واقعی ندارند نمایش داده نمی‌شوند. نمونهٔ نمایشی این ردیف‌ها در Storybook وجود دارد. پیش از انتشار، متن‌های شرایط و حریم خصوصی را با سیاست واقعی مجموعه تطبیق دهید.

## فایل‌های مهم

- `app/page.tsx`: لندینگ و تعاملات
- `app/landing-copy.ts`، `app/function-copy.ts`، `app/section-content.ts`: متن‌های سه زبان
- `src/components/footer`: کامپوننت‌های مستقل و Storybook
- `src/server`: ذخیره‌سازی، اعتبارسنجی و مدیریت نشست
- `app/api`: API واقعی
- `app/admin`: پنل مدیریت
- `API.md`: قرارداد API

```bash
npm run typecheck
npm test
npm run storybook
```
