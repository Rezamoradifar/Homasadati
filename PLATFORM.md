# پنل کاربری و مدیریت همای سعادت

این نسخه به SQLite واقعی روی دیسک سرور متصل است. نصب، محصول، موجودی کیف پول، فروش، رتبه یا درصد پورسانت نمونه ایجاد نمی‌کند. داده‌های تست فقط در دیتابیس‌های موقت و مستقل هستند.

- `/account`: ثبت‌نام، ورود، خرید، سفارش، شبکه، کیف پول، پروفایل و امنیت
- `/admin`: مدیریت با نقش‌های مستقل و کنترل دسترسی سمت API
- `/admin/legacy`: مدیریت درخواست‌های لندینگ قبلی با رمز قدیمی `ADMIN_PASSWORD`
- `/pages/[slug]`: محتوای منتشرشده توسط مدیر
- `/api/platform/*`: API پنل‌ها؛ پاسخ‌های خصوصی بدون کش


## ارتقای مدیریت محصول و داشبوردها

نسخه جدید علاوه بر داشبورد مالی، یک داشبورد جامع و چهار داشبورد گردشگری، زیبایی، صنایع‌دستی و هوش مصنوعی دارد. دسترسی این گزارش‌های مالی برای سوپرادمین و مدیر مالی است. داده‌ها شامل فروش، میانگین سفارش، برگشت وجه، وضعیت سفارش‌ها، روند فروش، ۱۰ محصول پرفروش، ۵۰ هشدار موجودی و ۳۰ سفارش قدیمی باز هستند. اشتراک فعال و انقضای هفت روز آینده نیز از دیتابیس خوانده می‌شود. کارت‌های فروش تاریخ ایجاد سفارش را مبنا دارند؛ نمودار و پرفروش‌ها تاریخ پرداخت را. موجودی و صف اجرا وضعیت جاری‌اند.

فرم محصول اکنون بخش‌بندی شده است:

| بخش | مشخصات قابل ثبت |
|---|---|
| شناسه و تنوع | SKU یکتا، بارکد، خانواده محصول، برند، مبدأ، تأمین‌کننده، رنگ، سایز و جنس |
| قیمت و انبار | قیمت فروش، بهای خرید خصوصی، قیمت قبل از تخفیف، موجودی مستقل و آستانه هشدار |
| ارسال | وزن بسته، ابعاد، توضیحات ارسال، ضمانت و مرجوعی |
| گردشگری | مقصد، مبدأ، تاریخ شروع/پایان، شب اقامت، هتل، وعده‌ها، برنامه روزانه، خدمات شامل/خارج از قیمت |
| زیبایی | ترکیبات، نوع پوست/مو، حجم، تاریخ انقضا، روش مصرف و هشدارها |
| صنایع‌دستی | هنرمند، تکنیک ساخت، جنس و نگهداری |
| اشتراک AI | ارائه‌دهنده، مدل‌ها، تعداد کاربر، سهمیه، روش تحویل و پیش‌نیازها |
| ارائه بین‌المللی | عنوان و توضیح فارسی/انگلیسی/عربی، ویژگی‌ها، ویدئو و فیلدهای عنوان/توضیح سئو |

فیلدهای سئو و ترجمه در این نسخه ذخیره و در مشخصات محصول قابل مشاهده‌اند؛ این به معنای ترجمه کامل رابط پنل یا ایجاد صفحات عمومی ایندکس‌پذیر برای هر محصول نیست. واحد محاسبات مالی همچنان تومان است. شروط ارسال، سهمیه و روش تحویل اطلاعات محصول هستند؛ موتور نرخ حمل‌ونقل بین‌المللی، مالیات کشورها و provisioning سرویس خارجی ایجاد نمی‌کنند.

هر تنوع قابل فروش یک رکورد محصول با SKU یکتا، قیمت و موجودی مستقل است. کد خانواده مشترک تنوع‌ها را به هم متصل می‌کند؛ مشتری با «تنوع‌های این محصول» موارد همان خانواده را می‌بیند. دکمه «ساخت تنوع جدید» مشخصات محصول را برای SKU جدید کپی می‌کند، موجودی را صفر و انتشار را غیرفعال می‌گذارد تا مدیر مقدار واقعی را وارد کند. برای اتصال تنوع‌ها کد خانواده یکسان در هر دو رکورد لازم است؛ تولید خودکار تمام ترکیب‌های رنگ/سایز وجود ندارد. SKU و مشخصات عمومی هنگام سفارش snapshot می‌شوند. اطلاعات بهای خرید، تأمین‌کننده و آستانه هشدار در API عمومی افشا نمی‌شود. فرم جدید هنگام ذخیره، تغییر هم‌زمان موجودی یا مشخصات را تشخیص می‌دهد و درخواست تازه‌سازی می‌کند.

بارگذاری تصویر واقعی از داخل فرم پشتیبانی می‌شود: JPEG/PNG/WebP، حداکثر ۸ مگابایت و ۲۵ میلیون پیکسل. فایل بازخوانی و به WebP با اندازه حداکثر ۲۴۰۰ پیکسل تبدیل می‌شود؛ فایل خام، SVG و تصویر متحرک نگهداری نمی‌شود. حداکثر ۱۲ تصویر در گالری؛ تصویر اول اصلی است. تصاویر در پوشه `media` کنار فایل دیتابیس و خارج از public ذخیره و از API با MIME ثابت ارائه می‌شوند. `platform:backup` این پوشه را نیز کپی می‌کند. برای بازیابی آن را کنار دیتابیس برگردانید. reverse proxy باید سقف body حداقل ۸ مگابایت داشته باشد (مثلاً `client_max_body_size 9m` در nginx). حذف نشانی از محصول، فایل روی دیسک را پاک نمی‌کند؛ پاکسازی فایل‌های بی‌استفاده در این نسخه خودکار نیست.

مهاجرت شماره ۲ جدول جداگانه مشخصات محصول را ایجاد می‌کند و ستون‌ها یا داده‌های قبلی محصول را حذف نمی‌کند. با اجرای `npm run platform:setup` یا اولین اتصال برنامه اعمال می‌شود.

مراجع طراحی فنی: [مدل تنوع محصول Shopify](https://help.shopify.com/en/manual/products/variants) و [محدودسازی ورودی تصویر در Sharp](https://sharp.pixelplumbing.com/api-constructor/).

## نصب روی سرور فعلی

پیش‌نیاز: Node.js 22، npm، دیسک دائمی و دامنه دارای HTTPS. اجرای پنل production روی آدرس HTTP فعلی `185.172.64.14:3000` برای ورود کافی نیست: کوکی حساب عمداً Secure است. ابتدا دامنه و TLS را روی reverse proxy سرور تنظیم کنید. کلیدهای سرویس را در گفت‌وگو یا GitHub قرار ندهید.

فرمان‌ها با root اجرا می‌شوند. ابتدا پشتیبان سازگار از دیتابیس فعلی و فایل تنظیمات بگیرید. در نصب فعلی، نسخه قدیمی ممکن است هنوز فرمان `platform:backup` نداشته باشد؛ برای اولین ارتقا از روش خاموش زیر استفاده کنید:

```bash
systemctl stop homay
install -d -m 700 /opt/homay/pre-platform-backup
cp -a /opt/homay/app/data /opt/homay/pre-platform-backup/
cp -a /opt/homay/app/.env.local /opt/homay/pre-platform-backup/
systemctl cat homay > /opt/homay/pre-platform-backup/homay-service.txt
sudo -u homay -H bash <<'INSTALL'
set -e
cd /opt/homay/app
git pull --ff-only
npm ci
npm run typecheck
npm test
INSTALL
```

اگر مسیر دیتابیس شما در `DATABASE_PATH` متفاوت است، همان فایل و فایل‌های WAL/SHM آن را پس از توقف سرویس پشتیبان بگیرید. پوشه پشتیبان اول را در ارتقای بعدی بازنویسی نکنید.

سپس مدیر اصلی را بسازید؛ اسکریپت ایمیل مدیر را می‌پرسد:

```bash
sudo -u homay -H bash -c 'cd /opt/homay/app && npm run platform:setup'
```

این فرمان یک کلید رمزنگاری دائمی در `.env.local` و رمز تصادفی مدیر در `/opt/homay/app/.platform-bootstrap.txt` با مجوز 0600 ذخیره می‌کند. فایل رمز را فقط در ترمینال خصوصی خود بخوانید. بعد از ورود، رمز را تغییر دهید، TOTP را فعال کنید و فایل رمز اولیه را حذف کنید. اجرای مجدد setup مدیر و اطلاعات موجود را بازنویسی نمی‌کند.

در `.env.local` مقدار `APP_ORIGIN` را دقیقاً برابر مبدا HTTPS سایت بدون `/` آخر قرار دهید. مقدار قدیمی HTTP در systemd بر فایل env اولویت دارد؛ با `systemctl edit homay` آن را نیز اصلاح کنید:

```ini
[Service]
Environment=APP_ORIGIN=https://YOUR-DOMAIN
ExecStart=
ExecStart=/usr/bin/node /opt/homay/app/node_modules/next/dist/bin/next start -H 127.0.0.1 -p 3000
```

`YOUR-DOMAIN` را با دامنه واقعی جایگزین کنید. reverse proxy باید درخواست‌ها را به `127.0.0.1:3000` بفرستد، Host اصلی را حفظ کند و TLS معتبر داشته باشد. فقط اگر proxy هدر `X-Forwarded-For` را خودش **بازنویسی** می‌کند و پورت 3000 از بیرون بسته است، `TRUST_PROXY=1` تنظیم کنید؛ در غیر این صورت صفر بماند. بدون proxy قابل اعتماد، محدودیت IP مشترک است و برای بار عمومی مناسب نیست.

```bash
sudo -u homay -H bash <<'BUILD'
set -e
cd /opt/homay/app
npm run build
test -f .next/BUILD_ID
BUILD
install -m 644 /opt/homay/app/deploy/homay-worker.service /etc/systemd/system/homay-worker.service
systemctl daemon-reload
systemctl restart homay
systemctl enable --now homay-worker
systemctl status homay homay-worker --no-pager
```

اگر build شکست خورد سرویس جدید را راه‌اندازی نکنید؛ خطای همان مرحله را رفع کنید یا نسخه و پشتیبان قبلی را بازیابی کنید. بدون SSH این تغییرات خودکار روی سرور شما انجام نمی‌شوند.

## راه‌اندازی واقعی از داخل پنل

۱. به `/admin` با ایمیل و رمز ایجادشده وارد شوید. در «تنظیمات» نام، لوگو و تماس را ثبت کنید.
۲. کلید Resend و فرستنده ایمیل تأییدشده، کلید Kavenegar، قالب OTP تأییدشده و شماره فرستنده اعلان را تنظیم کنید. شماره موبایل با پیش‌شماره کشور مثل `+98912…` پذیرفته می‌شود.
۳. Merchant ID زرین‌پال و مبدا HTTPS را تنظیم کنید. مبلغ داخلی تومان صحیح است؛ درخواست درگاه به ریال (×۱۰) ارسال می‌شود. پرداخت صرفاً پس از verify سمت سرور ثبت می‌شود.
۴. در سیاست مالی، درصد مستقیم، سطوح، باینری، سقف پورسانت هر سفارش، آستانه‌های هشدار و حداقل/حداکثر برداشت را با سیاست واقعی کسب‌وکار وارد کنید. **هیچ نرخ پیش‌فرض عملیاتی فعال نیست**؛ تا ثبت سیاست، خرید انجام نمی‌شود.
۵. رتبه‌ها، آستانه‌ها، مأموریت‌ها، دسته‌ها، برچسب‌ها و محصولات واقعی چهار حوزه را ثبت و منتشر کنید. تصویر با بارگذاری مستقیم در فرم یا URL معتبر HTTPS و مسیر `/assets/` مدیریت می‌شود.
۶. کاربران کارکنان را پس از ثبت‌نام به نقش مناسب ارتقا دهید. مدیر نمی‌تواند نقش یا وضعیت خودش را تغییر دهد.
۷. یک خرید واقعی کم‌مبلغ، callback، فاکتور، لغو، آزادسازی پورسانت و برداشت را روی محیط staging/حساب درگاه مجاز خود بررسی کنید؛ تست‌های خودکار جای تأیید اتصال حساب شما را نمی‌گیرند.

کلیدهای API و راز TOTP با AES-256-GCM در دیتابیس رمز می‌شوند. مقدار کلیدها در پاسخ تنظیمات و Audit برگردانده نمی‌شود. `PLATFORM_MASTER_KEY` باید ثابت بماند و همراه بکاپ محفوظ باشد؛ تعویض آن بدون مهاجرت، اطلاعات رمز‌شده را غیرقابل خواندن می‌کند.

## منطق مالی اجراشده

- تمام مبالغ تومان صحیح‌اند؛ درصدها در DB به basis point ذخیره می‌شوند (۱۰۰ = ۱٪)، محاسبه بدون اعشار شناور و گردکردن رو به پایین است.
- سیاست و شرایط محصول هنگام سفارش ثبت می‌شوند؛ ویرایش بعدی نرخ‌ها روی سفارش قبلی اثر ندارد.
- پورسانت فقط بابت خرید تأییدشده است؛ ترتیب مصرف سقف سفارش: مستقیم، سطوح معرف، پاداش رتبه، سپس باینری. مبلغ اضافی از سقف پرداخت نمی‌شود.
- رتبه از فروش خالص شخصی/گروهی عمر حساب محاسبه می‌شود؛ پاداش رتبه درصد فروش واجد شرایط است، نه هدیه ثابت ارتقا.
- درخت معرف و جای‌گذاری باینری جدا هستند. باینری روی حجم واقعی فروش تومانی دو شاخه، با carry و تطبیق FIFO کار می‌کند؛ UV وجود ندارد. جابه‌جایی، سوابق تسویه‌شده را بازنویسی نمی‌کند.
- پورسانت تا پایان مهلت لغو pending است؛ worker و عملیات مالی آن را آزاد می‌کنند. برای باینری پایان مهلت هر دو فروش ملاک است.
- ثبت برداشت، موجودی آزاد را به held می‌برد. تأیید مدیر در یک تراکنش held را کسر و درخواست را approved می‌کند؛ paid تنها پس از ثبت شماره پیگیری انتقال واقعی بانک توسط مدیر مالی ثبت می‌شود. **انتقال بانکی خودکار پیاده‌سازی نشده است.** رد قبل از پرداخت وجه را آزاد می‌کند.
- لغو معتبر، موجودی کالا را برمی‌گرداند و **وجه را به کیف پول** واریز می‌کند؛ UI قبل از لغو این موضوع را تأیید می‌گیرد. برگشت مستقیم به کارت از API درگاه انجام نمی‌شود. پرداخت معتبر پس از انقضای سفارش نیز فقط یک‌بار به کیف پول برمی‌گردد.
- برگشت فروش، پورسانت مستقیم/سطوح/رتبه/باینری مربوط را معکوس می‌کند. اگر پورسانت قبلاً برداشت شده باشد بدهی ثبت و از اعتبارهای بعدی کسر می‌شود؛ موجودی منفی یا برداشت تازه با بدهی مجاز نیست.
- سلامت مالی، هم تعهد پورسانت و هم پرداخت نقدی نسبت به فروش خالص را بررسی می‌کند. وضعیت بحرانی یا توقف دستی برداشت و تأیید/پرداخت جدید را مسدود می‌کند. نسبت بدون فروش «تعریف‌نشده» است و وجود پرداخت/تعهد بدون فروش وضعیت بحرانی ایجاد می‌کند. نسبت روزانه ممکن است به‌دلیل فاصله فروش و تسویه بزرگ باشد؛ کنترل عملیاتی بر کل تاریخچه است.
- موجودی، سفارش، پورسانت، برداشت و Audit حساس با `BEGIN IMMEDIATE` و قفل نوشتن SQLite اتمیک هستند. کلید یکتای رویداد و idempotency از اعمال مالی تکراری جلوگیری می‌کند. Audit و ledger از مسیر SQL عادی قابل ویرایش/حذف نیستند؛ دسترسی root دیتابیس همچنان قدرت مدیریتی دارد.

## نقش‌ها

| بخش | سوپرادمین | محتوا | پشتیبانی | مالی |
|---|---|---|---|---|
| محصول، دسته، محتوا، مأموریت | بله | بله | خیر | خیر |
| سفارش و تغییر وضعیت | بله | خیر | بله | بله |
| بازگشت وجه، برداشت، نرخ، رتبه، گزارش | بله | خیر | خیر | بله |
| کاربران عادی و پرچم مشکوک | بله | خیر | بله | خیر |
| نقش کارکنان، شبکه، تنظیمات و Audit | بله | خیر | خیر | خیر |

همه این محدودیت‌ها در API اعمال می‌شوند. مسدودسازی حساب نشست‌های آن را باطل می‌کند. تغییر رمز، بازیابی رمز و خروج همه دستگاه‌ها نشست‌های قبلی را لغو می‌کنند. OTP پنج دقیقه اعتبار و پنج تلاش دارد؛ TOTP اختیاری است و کد استفاده‌شده دوباره پذیرفته نمی‌شود.

## دامنه و محدودیت‌های مشخص

- پنل‌ها فارسی/RTL هستند؛ لندینگ چندزبانه قبلی حفظ شده است. ترجمه کامل پنل‌ها به انگلیسی/عربی جزو این نسخه نیست.
- گردشگری مقصد/تور/اقامتگاه را به‌صورت نوع محصول، ظرفیت و سفارش مدیریت می‌کند؛ اتصال تأمین‌کننده هتل/پرواز، تقویم ظرفیت روزانه و صدور بلیت خارجی ندارد.
- اشتراک AI خرید، تاریخ انقضا، تمدید و لغو را در دیتابیس واقعی مدیریت می‌کند؛ تحویل حساب یا provisioning سرویس AI خارجی نیازمند اتصال به ارائه‌دهنده منتخب است. لغو اشتراک جلوی تمدید اختیاری آینده را مشخص می‌کند؛ برداشت دوره‌ای خودکار وجود ندارد.
- اعلان‌های ایمیل/SMS صف واقعی با retry دارند. Resend کلید idempotency دارد؛ SMS در خرابی دقیقاً بین ارسال و ثبت نتیجه می‌تواند تکرار شود. شکست نهایی در `p_outbox` و لاگ worker قابل بررسی است.
- تشخیص مشکوک قاعده‌محور است (ثبت‌نام چندگانه/IP، خرید و دعوت سریع)، نه تضمین کشف تقلب یا تطبیق هویت بانکی. مالکیت شبا خودکار استعلام نمی‌شود؛ قالب و checksum بررسی می‌شود.
- داده کسب‌وکار و ارقام پنل‌ها DB-backed هستند. متن رابط، چهار حوزه و enum وضعیت‌ها تنظیمات ثابت برنامه‌اند. داده fixture فقط در فایل‌های test وجود دارد؛ کاتالوگ و نرخ نمایشی seed نمی‌شود.
- استقرار یک سرور با دیسک محلی برای SQLite طراحی شده است. چند replica روی فایل‌سیستم شبکه یا serverless بدون دیسک دائمی پشتیبانی نمی‌شود. پیش از مقیاس بزرگ، مهاجرت کنترل‌شده به PostgreSQL و تست بار لازم است.
- گزارش روزانه و فیلتر تاریخ بر مبنای تاریخ میلادی/UTC است؛ نمایش تاریخ رویداد فارسی با منطقه تهران و «این ماه» داشبورد بر مبنای ماه شمسی تهران است.

## پشتیبان، پایش و بازگردانی

```bash
sudo -u homay -H bash -c 'cd /opt/homay/app && npm run platform:backup'
journalctl -u homay -u homay-worker -n 100 --no-pager
```

بکاپ از API online backup SQLite استفاده می‌کند و `.env.local` را به فایل خصوصی `environment.private` کپی می‌کند. اگر کلید اصلی از systemd یا secret manager آمده، جداگانه همان مقدار را نیز پشتیبان بگیرید. بکاپ را خارج از سرور با دسترسی محدود نگه دارید. برای بازیابی هر دو سرویس را متوقف کنید، دیتابیس و کلید همان نسخه را بازیابی کنید، فایل‌های WAL/SHM قدیمی را با دیتابیس تازه مخلوط نکنید، مالکیت homay و مجوز 0600 را حفظ و سپس سرویس‌ها را راه‌اندازی کنید. نسخه کد باید با schema بکاپ سازگار باشد.

## نتیجه اعتبارسنجی این نسخه

- `npm run typecheck` و `npm run build`: موفق؛ خروجی production ساخته شد.
- `npm test`: ۴۵ تست موفق، شامل ۱۳ تست مالی، ۷ تست API جریان کامل، ۱۰ تست کاتالوگ/داشبورد/تصویر، ۳ تست رابط متصل به SQLite و ۱۲ تست قبلی.
- سناریوهای مالی: خرید ساده، چند سطح، تعادل و carry باینری، رتبه جدید، سقف پرداخت، دو پردازش مستقل برای برداشت هم‌زمان، rollback هنگام شکست Audit، برگشت پورسانت، پرداخت دیرهنگام و بدهی پس از تأیید برداشت.
- مسیر یکپارچه API: OTP ثبت‌نام ← سفارش ← verify پرداخت ← پورسانت ← برداشت ← تأیید مدیر و کسر held، همراه کنترل دسترسی، مالکیت، CSRF، PDF و CSV/XLSX.
- رابط: داده خالی واقعی، ذخیره پروفایل، ایجاد/ویرایش محصول، دسترسی غیرمجاز و خطای شبکه؛ تمام آن‌ها به API و DB موقت واقعی وصل‌اند.
- فقط مرز شبکه ارائه‌دهندگان در تست‌های خودکار شبیه‌سازی شده است. پرداخت بانکی، ایمیل و SMS واقعی ارسال نشده؛ تست مرورگری کامل ورود تا بانک انجام نشده است. اجرای موفق تست‌ها تضمین نبود همه باگ‌ها نیست.

ساختار کد: `src/platform/schema.ts`، `finance.ts`، `security.ts`، `providers.ts`، `api.ts`، `maintenance.ts`، پنل‌ها و فایل‌های `*.test.*`. مسیر Next در `app/api/platform/[...path]/route.ts` با runtime Node قرار دارد.
