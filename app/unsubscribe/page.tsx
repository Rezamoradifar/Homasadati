'use client';

import Localized from "../../src/i18n/Localized";
import {useEffect,useState,type FormEvent} from 'react';
import '../admin/style.css';
export default function Unsubscribe(){const [token,setToken]=useState(''),[done,setDone]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');useEffect(()=>{setToken(new URLSearchParams(window.location.search).get('token')||'');},[]);async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{const r=await fetch('/api/unsubscribe',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})});if(!r.ok)throw Error();setDone(true);}catch{setError('لغو انجام نشد؛ کد را بررسی کنید و دوباره تلاش کنید. Cancellation failed.');}finally{setBusy(false);}}return <Localized><main className="admin" dir="rtl"><a href="/">همای سعادت · Home</a><form onSubmit={submit} className="admin-login"><h1>لغو درخواست خبرنامه</h1><p lang="en">Cancel your newsletter request</p>{done?<p role="status">درخواست لغو پردازش شد. Your cancellation request has been processed.</p>:<><label>کد خصوصی · Private token<input required minLength={64} maxLength={64} value={token} onChange={e=>setToken(e.target.value)} dir="ltr"/></label><button disabled={busy}>{busy?'…':'لغو خبرنامه · Unsubscribe'}</button><p role="alert">{error}</p></>}</form></main></Localized>;}
