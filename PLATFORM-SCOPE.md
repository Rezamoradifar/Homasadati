# وضعیت اجرای پلتفرم باشگاه مشتریان همای سعادت

این سند شرح فنی مالک را به امکانات واقعی کد متصل می‌کند. این نسخه در شاخه توسعه آماده شده است؛ به معنی انتشار روی homanets.com یا اتصال سرویس‌های واقعی نیست.

## پوشش شرح فنی

| حوزه              | پیاده‌سازی در این نسخه                                                                                                                                                 | نیاز عملیاتی پیش از فعال‌سازی عمومی                                                                                     |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| معماری            | Next.js، ماژول‌های مستقل سرور، REST API، SQLite WAL و تراکنش IMMEDIATE، worker مستقل                                                                                   | این استقرار یک میزبان است؛ چند میزبان هم‌زمان به پایگاه‌داده و صف مناسب آن معماری نیاز دارد                             |
| محیط‌ها و استقرار | Dockerfile، Compose، جداسازی نام پروژه/پورت/داده Staging و Production، CI                                                                                              | اجرای image روی Docker، انتقال دیتابیس موجود و تأیید Nginx/SSL در سرور                                                  |
| سایت              | لندینگ و فروشگاه موجود، سبد و محصول، صفحه پذیرندگان، صفحه زنده `/club` با قواعد و مزایای فعال                                                                          | بررسی کامل تمام صفحات با محتوای واقعی و دستگاه‌های مالک                                                          |
| پنل کاربر         | حساب، سفارش، امتیازات و انقضا، سطح باشگاه، درخواست و لغو مزیت، شبکه و دعوت، باینری، کیف پول، کارت سفر، تیکت پشتیبانی، اعلان‌ها و امنیت                                 | محتوای واقعی محصولات و مزایا، تنظیم شرایط مصوب کسب‌وکار                                                                 |
| پنل مدیریت        | میز کار با جست‌وجوی بخش‌ها، محصولات و موجودی، سفارش‌ها، کاربران، گزارش‌ها، محتوا، امتیاز، مزایا، پذیرندگان و تسویه                                                     | تعیین اپراتورها و اختصاص مجوزهای مناسب                                                                                  |
| دسترسی            | نقش‌های اصلی و اختصاصی، مجوز خواندن/نوشتن منبع، تخصیص و لغو فوری در API، ثبت Audit                                                                                     | تعریف نقش‌های واقعی سازمان؛ مدیریت کلیدها و نقش‌ها فقط برای مدیر اصلی                                                   |
| باینری            | Sponsor مستقل از Placement، درخت چپ/راست، دفتر حجم، نسبت قابل تنظیم، تسویه فوری/روزانه/هفتگی، سقف روزانه تهران، شرط خرید/معرف فعال، انقضای حجم و شبیه‌ساز              | ثبت و بررسی نرخ‌ها و قواعد مصوب؛ نسبت پیش‌فرض همان ۱:۱ قبلی است                                                         |
| امتیاز و Loyalty  | سیاست امتیاز خرید، آزادسازی پس از مهلت لغو، نگهداری قواعد هر سفارش، انقضا، سطوح مبتنی بر خرید، دفتر غیرقابل‌ویرایش، مدیریت کسری مرجوعی                                 | امتیاز خودکار پیش‌فرض غیرفعال است؛ مبلغ واحد و امتیاز باید توسط مدیر وارد شوند                                          |
| مزایا             | هزینه امتیازی، ظرفیت، رزرو هم‌زمان با کسر امتیاز، تحویل/لغو، بازگشت با انقضای اصلی                                                                                     | ثبت مزایای قابل تحویل و مسئول عملیات                                                                                    |
| پذیرندگان         | معرفی عمومی، قرارداد مدت‌دار، مسئول حساب، سهم فروش، اتصال محصول، پنل سفارش همان پذیرنده، دفتر تسویه و ثبت پرداخت بانکی با تأیید دو مدیر                                | قراردادها، حساب‌های واقعی و ثبت شماره پیگیری پرداخت انجام‌شده توسط دو مدیر مستقل؛ انتقال وجه خودکار بانک پیاده نشده است |
| مالی فروشگاه      | سفارش، سبد چندقلمی، موجودی، کیف پول، پرداخت/تأیید و مرجوعی، فاکتور و پورسانت                                                                                           | کلید و آزمون واقعی درگاه، تطبیق بانکی                                                                                   |
| اعلان             | اعلان شخصی، ارسال هدفمند مدیریتی تا ۱۰۰ عضو، صف ایمیل/پیامک با رعایت ترجیح کاربر و تکرار امن                                                                           | تنظیم سرویس پیامک و ایمیل و آزمون تحویل واقعی                                                                           |
| امنیت             | نشست تصادفی هش‌شده با کوکی HttpOnly، چرخش نشست، OTP، 2FA، اتصال گوگل، کنترل دسترسی، اعتبارسنجی، SQL پارامتری، CSRF و هدرهای امنیتی                                     | کلیدهای واقعی، ممیزی مستقل و آزمون نفوذ Production                                                                      |
| API و مستندات     | APIهای عملیاتی، ساخت قرارداد OpenAPI ماژول‌های جدید از Zod، راهنمای مسیرها و خطاها                                                                                     | در `docs/API.md` و `docs/openapi.json`؛ تمام APIهای قدیمی هنوز تفصیلاً در OpenAPI نیستند                                |
| پشتیبان و پایش    | Backup سازگار SQLite، manifest و checksum، آزمون بازیابی در کپی موقت، بررسی رمزگشایی و تطبیق کیف پول، health توکن‌دار، وضعیت worker، ابزار Restic و پایش زمان‌بندی‌شده | تنظیم مقصد Restic و گیرنده هشدار، نصب timerها و بازیابی از مخزن بیرونی واقعی                                            |

## قواعد دقیق مالی و شبکه

1. امتیاز، کیف پول نقدی، حجم باینری و اعتبار سفر واحدهای جدا هستند.
2. پورسانت مستقیم، سطحی و رتبه از بودجه سفارش کسر می‌شود؛ باینری فقط از بودجه باقی‌مانده استفاده می‌کند. سهم پذیرنده و سقف پورسانت شبکه نمی‌توانند مجموعاً از مبلغ فروش بیشتر شوند.
3. قواعد باینری، امتیاز و قرارداد پذیرنده در زمان ایجاد سفارش ذخیره می‌شوند؛ تغییر تنظیمات، سفارش قبلی را بازنویسی نمی‌کند.
4. نسبت باینری مثلاً ۲:۱، برای هر واحد تطبیق دو واحد چپ و یک واحد راست مصرف می‌کند؛ نرخ روی واحد تطبیق اعمال می‌شود. محاسبه عدد صحیح است. حجم‌های خرد چند سفارش با ترتیب FIFO برای تکمیل نسبت جمع می‌شوند؛ سهم هر سند در تطبیق و مرجوعی جدا ثبت می‌شود.
5. سقف روزانه باینری شامل پورسانت معلق و آزادشده است و بر اساس روز تهران محاسبه می‌شود. مرجوعی، پورسانت باطل‌شده را از جمع خارج می‌کند.
6. زمان‌بندی باینری هنگام ایجاد سفارش ثبت می‌شود: فوری، روزانه یا هفتگی به وقت تهران. worker می‌تواند بدون خرید تازه از بودجه باقی‌مانده سفارش پرداخت‌شده تسویه کند. حجم انتقالی بدون بودجه واقعی وعده پرداخت نیست؛ سقف/صلاحیت در زمان اجرای واقعی کنترل می‌شود.
7. حجم منقضی حذف نمی‌شود؛ در گزارش جداست و دوباره وارد تعادل نمی‌شود. مرجوعی نسبت واقعی مصرف چپ و راست را برمی‌گرداند و تاریخ انقضای اصلی را نگه می‌دارد.
8. کسب خودکار امتیاز پیش‌فرض غیرفعال است. امتیاز خرید واجد شرایط پس از مهلت لغو آزاد می‌شود. انقضای امتیاز بر اساس موعد آزادسازی است.
9. لغو مزیت، تاریخ اعتبار امتیاز را تمدید نمی‌کند. مرجوعی خرید، مزیت رزروشده و تحویل‌نشده وابسته را خودکار لغو می‌کند. اگر مزیت تحویل شده باشد، کسری امتیاز آشکار ثبت و از امتیازهای بعدی جبران می‌شود.
10. سطح امتیازی از مجموع امتیاز خریدهای غیرمرجوع محاسبه می‌شود؛ مصرف و انقضا آن را کاهش نمی‌دهند. رتبه شبکه و کارت سفر جدا هستند.
11. تسویه پذیرنده بعد از تحویل سفارش و پایان مهلت لغو قابل ثبت است. مرجوعی پس از تسویه، مانده منفی ایجاد می‌کند که از فروش بعدی جبران می‌شود. هیچ انتقال بانکی صرفاً با این فرم اجرا نمی‌شود.
12. اسناد امتیاز، پول و تسویه پذیرنده و Audit از مسیر کاربردی ویرایش یا حذف نمی‌شوند؛ تصحیح مالی با سند جبرانی است.

## وضعیت آزمون

- کل مجموعه آزمون پروژه: **۱۵۳ آزمون در ۱۹ فایل موفق**، شامل API، امنیت، سفارش، پرداخت، امتیاز، باینری، پذیرنده، چندپردازشی SQLite و گردش رابط کاربری با API واقعی آزمایشی.
- `npm run build` نهایی، شامل بررسی TypeScript، موفق بود.
- اجرای واقعی نسخه ساخته‌شده روی loopback: صفحات `/club`، `/merchants`، `/admin` و `/account` و API عمومی پاسخ ۲۰۰ دادند؛ API مدیریتی بدون ورود ۴۰۱ و نوشتن با Origin بیگانه ۴۰۳ داد.
- بازیابی یک Backup آزمایشی با migration نسخه ۱۱، checksum، integrity، کلیدهای خارجی، رمزگشایی و تطبیق دفتر کیف پول موفق بود؛ فایل زنده تغییر نکرد.
- ۱۲۰ درخواست محلی با هم‌زمانی ۶ بدون شکست: میانه ۲۱ میلی‌ثانیه، صدک ۹۵ برابر ۱۴۸ میلی‌ثانیه. داده آزمایشی سبک است و نتیجه تضمین عملکرد Production نیست.
- آزمون شبیه‌ساز از همان تابع عدد صحیح موتور اصلی استفاده می‌کند و ایجاد سند مالی نمی‌کند.
- آزمون Chromium روی build واقعی: ثبت تیکت، یادداشت داخلی، پاسخ عمومی و مخفی‌ماندن یادداشت از عضو موفق بود؛ صفحه زمان‌بندی در عرض‌های ۳۲۰، ۳۹۰، ۷۶۸ و ۱۴۴۰ بدون سرریز افقی و خطای JavaScript بررسی شد. تصاویر پنل پشتیبانی و زمان‌بندی نیز بازبینی شدند. این بررسی به معنی آزمون تصویری کامل تمام صفحات نیست.
- آزمون بار محلی فقط یک نمونه محدود خواندن API است و برآورد ظرفیت سرور اصلی محسوب نمی‌شود.
- اجرای Docker، بررسی تصویری کامل، آزمون بار Production، کلیدهای واقعی provider و انتشار سرور در این محیط تأیید نشده‌اند.

## فایل‌های تحویل

- `docs/API.md`: مسیرها، مجوزها و چرخه وضعیت‌ها.
- `docs/openapi.json`: قرارداد ساختاری ماژول‌های جدید.
- `deploy/CONTAINERS.md`: روش تفکیک محیط‌ها و انتقال داده موجود.
- `deploy/nginx-homanets.conf`: الگوی اتصال دامنه و گواهی موجود به برنامه.
- `scripts/platform-restore-drill.ts`: آزمون بازیابی بدون دست‌زدن به فایل‌های زنده.
- `scripts/platform-read-benchmark.mjs`: آزمون محدود خواندن HTTP فقط روی loopback.

احراز هویت این پروژه با نشست امن کوکی انجام می‌شود؛ JWT و Refresh Token جداگانه به آن تحمیل نشده است. `/auth/refresh` همان نشست امن را چرخش می‌دهد و توکن قدیمی را باطل می‌کند.

## ارتقای عملیات

تیکت متنی، یادداشت داخلی و مسئول رسیدگی، تأیید دو نفره برداشت/تسویه پذیرنده، دوره روزانه/هفتگی باینری و ابزار پشتیبان Restic/پایش اضافه شده‌اند. راهنمای دقیق، محدودیت‌های بودجه و ورودی‌های لازم در [عملیات](docs/OPERATIONS.md) است. انتقال بانکی خودکار، ارسال واقعی ارائه‌دهندگان و نصب روی VPS هنوز بدون دسترسی و تنظیمات مالک قابل تأیید نیستند.

آزمون‌های جدید، مستقل‌بودن دو تأییدکننده، منع تأیید ذی‌نفع، تکرار امن، بودجه/سقف/مرجوعی در دوره باینری، حریم تیکت، بازیابی بکاپ و قرارداد subprocess ابزار Restic را پوشش می‌دهند. آزمون رمزگذاری/انتقال Restic به مخزن بیرونی واقعی و دریافت هشدار بیرونی به تنظیمات مقصد نیاز دارد.
